---
import MainLayout from '../layouts/MainLayout.astro';
import webProjects from '../data/web-projects.json';
---

<MainLayout 
  title="Web Design Portfolio - Shawn Paps Media"
  description="Bold, fast, conversion-focused websites for brands with grit. View our web design portfolio featuring barbers, coffee shops, construction companies, and creators."
>
  <!-- Hero Section -->
  <section class="py-24 md:py-32 lg:py-40 px-6 md:px-12 bg-black pt-32">
    <div class="max-w-7xl mx-auto text-center max-w-4xl">
      <h1 class="text-5xl md:text-6xl lg:text-7xl font-black text-white mb-6">
        Web Design
      </h1>
      <p class="text-xl text-steel-400 leading-relaxed mb-8">
        Bold, fast, conversion-focused websites built with modern technology. 
        Your site should work as hard as you do - attracting customers, building your brand, and driving revenue.
      </p>
    </div>
  </section>

  <!-- Tech Stack -->
  <section class="py-24 md:py-32 lg:py-40 px-6 md:px-12 bg-black-soft border-y border-concrete">
    <div class="max-w-7xl mx-auto max-w-5xl">
      <h2 class="text-2xl font-bold text-white text-center mb-8">
        Built With Modern Tech
      </h2>
      <div class="flex flex-wrap justify-center gap-6">
        {['Astro', 'React', 'Next.js', 'Tailwind CSS', 'Shopify', 'Stripe', 'Cloudflare'].map(tech => (
          <div class="px-6 py-3 bg-black border border-concrete rounded-lg text-white font-semibold">
            {tech}
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Projects Grid -->
  <section class="py-24 md:py-32 lg:py-40 px-6 md:px-12 bg-black">
    <div class="max-w-7xl mx-auto">
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
        {webProjects.map((project, index) => (
          <div class="group relative overflow-hidden rounded-lg bg-black-soft border border-concrete hover:border-rust-500 transition-all">
            <!-- Project Image -->
            <div class="relative aspect-[16/10] overflow-hidden">
              <img 
                src={project.thumbnailUrl} 
                alt={project.title}
                class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-110"
              />
              <div class="absolute inset-0 bg-gradient-to-t from-black-deep via-black-deep/50 to-transparent opacity-80"></div>
              
              <!-- Quick Info Overlay -->
              <div class="absolute bottom-0 left-0 right-0 p-6">
                <h3 class="text-2xl font-black text-white mb-2">{project.title}</h3>
                <p class="text-steel-400 text-sm">{project.client} · {project.industry}</p>
              </div>
            </div>

            <!-- Project Details -->
            <div class="p-6 space-y-4">
              <p class="text-steel-400 leading-relaxed">
                {project.description}
              </p>

              <!-- Tech Stack Tags -->
              <div class="flex flex-wrap gap-2">
                {project.techStack.map(tech => (
                  <span class="px-3 py-1 bg-concrete text-white text-xs rounded-full">
                    {tech}
                  </span>
                ))}
              </div>

              <!-- Features -->
              <div class="space-y-2">
                <p class="text-white font-semibold text-sm">Key Features:</p>
                <div class="flex flex-wrap gap-2">
                  {project.features.map(feature => (
                    <span class="text-steel-400 text-xs">• {feature}</span>
                  ))}
                </div>
              </div>

              <!-- Results -->
              {project.result && (
                <div class="pt-4 border-t border-concrete">
                  <p class="text-rust-500 font-bold text-sm mb-2">Results:</p>
                  <p class="text-white text-sm">{project.result}</p>
                </div>
              )}

              <!-- CTA -->
              {project.liveUrl && (
                <a 
                  href={project.liveUrl}
                  target="_blank"
                  rel="noopener noreferrer"
                  class="inline-flex items-center gap-2 text-rust-500 hover:text-rust-400 font-bold transition-colors mt-4"
                >
                  View Live Site
                  <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"/>
                  </svg>
                </a>
              )}
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Process Section -->
  <section class="py-24 md:py-32 lg:py-40 px-6 md:px-12 bg-black-soft">
    <div class="max-w-7xl mx-auto max-w-5xl">
      <h2 class="text-4xl md:text-5xl lg:text-6xl font-black text-white text-center mb-16">
        Our Web Design Process
      </h2>
      
      <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
        <div class="space-y-4">
          <div class="w-12 h-12 rounded-full bg-rust-500 flex items-center justify-center text-white font-black text-xl">
            1
          </div>
          <h3 class="text-xl font-bold text-white">Strategy</h3>
          <p class="text-steel-400 text-sm">
            We start by understanding your business goals, target audience, and competitive landscape.
          </p>
        </div>
        
        <div class="space-y-4">
          <div class="w-12 h-12 rounded-full bg-rust-500 flex items-center justify-center text-white font-black text-xl">
            2
          </div>
          <h3 class="text-xl font-bold text-white">Design</h3>
          <p class="text-steel-400 text-sm">
            Bold, custom designs that reflect your brand personality and convert visitors into customers.
          </p>
        </div>
        
        <div class="space-y-4">
          <div class="w-12 h-12 rounded-full bg-rust-500 flex items-center justify-center text-white font-black text-xl">
            3
          </div>
          <h3 class="text-xl font-bold text-white">Development</h3>
          <p class="text-steel-400 text-sm">
            Built with modern tech for blazing-fast speed, perfect mobile experience, and easy management.
          </p>
        </div>
        
        <div class="space-y-4">
          <div class="w-12 h-12 rounded-full bg-rust-500 flex items-center justify-center text-white font-black text-xl">
            4
          </div>
          <h3 class="text-xl font-bold text-white">Launch & Growth</h3>
          <p class="text-steel-400 text-sm">
            We launch strategically and track results. Your site should grow your business, and we measure that.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- What's Included -->
  <section class="py-24 md:py-32 lg:py-40 px-6 md:px-12 bg-black">
    <div class="max-w-7xl mx-auto max-w-4xl">
      <h2 class="text-4xl md:text-5xl lg:text-6xl font-black text-white text-center mb-12">
        What's Included
      </h2>
      
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        {[
          'Custom, bold design that matches your brand',
          'Mobile-first responsive development',
          'Blazing-fast page load speeds (Core Web Vitals)',
          'SEO optimization from day one',
          'Integrated analytics and tracking',
          'Contact forms and lead capture',
          'E-commerce (if needed)',
          'Content management system',
          'SSL security certificate',
          'Hosting and domain setup',
          'Training and documentation',
          'Post-launch support',
        ].map(item => (
          <div class="flex items-start gap-3">
            <svg class="w-6 h-6 text-rust-500 flex-shrink-0 mt-1" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
            </svg>
            <span class="text-white">{item}</span>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA -->
  <section class="py-24 md:py-32 lg:py-40 px-6 md:px-12 bg-gradient-to-br from-rust-500 to-rust-400">
    <div class="max-w-7xl mx-auto text-center">
      <h2 class="text-4xl md:text-5xl lg:text-6xl font-black text-white mb-6">
        Ready to Build Your Site?
      </h2>
      <p class="text-xl text-white/90 max-w-2xl mx-auto mb-12">
        Let's create a website that works as hard as you do. Fast, bold, and built to convert.
      </p>
      <a 
        href="/contact"
        class="inline-block bg-black hover:bg-black-soft text-white font-bold text-lg px-12 py-4 rounded-lg transition-all hover:scale-105"
      >
        Start Your Web Project
      </a>
    </div>
  </section>
</MainLayout>
